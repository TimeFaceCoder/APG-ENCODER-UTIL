1.cook_encryp_rk.exe是加密文件的可执行文件，输入参数为要加密文件的路径，会在同路径下输出同名后缀为APG的加密后文件；
2.batch_encryption.bat 为一个调用cook_encryp_rk.exe进行文件夹遍历MP4进行加密的批处理文件，你们加密时候可以参考修改。



将扩展名为mp4的文件复制到当前目录，再运行bat文件，会自动将当前目录MP4文件转换为APG文件。